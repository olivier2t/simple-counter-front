on:
  workflow_dispatch:
    inputs:
      create_resource_group:
        description: 'Whether to create RG'
        required: false
        type: boolean

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Create RG: $CRG"
        env:
          CRG: ${{ inputs.create_resource_group }}
